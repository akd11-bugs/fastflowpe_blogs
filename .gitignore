build
dist / media
node_modules
.DS_Store
.env
.next
next-env.d.ts
.vercel

# Payload default media upload directory — committed for now so existing
# uploads survive a Vercel deploy (local-disk storage doesn't persist there).
# Uncomment once Media moves to a real cloud storage adapter (e.g.
# @payloadcms/storage-vercel-blob).
# public/media/

public/robots.txt
public/sitemap*.xml


# Playwright
node_modules/
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/

/generated/prisma
*.tsbuildinfo
