import React from 'react'

/** Collapsed-nav icon in the admin panel — the FastFlowPe icon mark. */
export const Icon: React.FC = () => (
  // eslint-disable-next-line @next/next/no-img-element
  <img alt="FastFlowPe" height={24} src="/favicon.svg" width={24} />
)

export default Icon
