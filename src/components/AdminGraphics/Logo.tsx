import React from 'react'

/** Login-screen logo in the admin panel — the FastFlowPe icon mark. */
export const Logo: React.FC = () => (
  // eslint-disable-next-line @next/next/no-img-element
  <img alt="FastFlowPe" height={64} src="/favicon.svg" width={64} />
)

export default Logo
